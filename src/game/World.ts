import { Iterable } from "./Iterable";
/**
 *  Связывающий класс. внутри него и будет протекать собственно игра.
 */
export class World implements Iterable {
    constructor() {}
    public init() {}
    public update() {}
}
