import { Iterable } from "./Iterable";
/**
 *  Класс вещей которые лежат на игровой доске, могут выпадать из монстрв и перелетать
 */
export class Thing implements Iterable {
    constructor() {}
    public init() {}
    public update() {}
}
